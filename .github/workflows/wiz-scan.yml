name: Build and Push Docker Image

on:
  pull_request:
    branches:
      - main # Or your main branch

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      imageid: ${{ steps.build-push.outputs.imageid }}
      digest: ${{ steps.build-push.outputs.digest }}
    steps:
      - uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and Push Docker Image
        id: build-push
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:${{ github.sha }}
            ghcr.io/${{ github.repository }}:latest
  wiz-cli-image-scan:
    name: "Wiz-cli Image Scan"
    runs-on: ubuntu-latest
    needs: build
    env:
      SCAN_PATH: "." # Set the relative path in the repo to build the image from
      POLICY: "Default vulnerabilities policy" # Set the desired Wiz CLI policy to use
      WIZ_ENV: "fedramp"

    # Use the Bash shell regardless whether the GitHub Actions runner is ubuntu-latest, macos-latest, or windows-latest
    defaults:
      run:
        shell: bash

    steps:
      # Checkout the repository to the GitHub Actions runner
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Use Image ID
        run: |
          echo "The image ID is: ${{ needs.build.outputs.imageid }}"
          echo "The image Digest is: ${{ needs.build.outputs.digest }}"

      - name: Download Wiz CLI
        run: curl -o wizcli https://downloads.wiz.io/wizcli/latest/wizcli-linux-amd64 && chmod +x wizcli

      - name: Authenticate to Wiz
        run: ./wizcli auth --id "$WIZ_CLIENT_ID" --secret "$WIZ_CLIENT_SECRET"
        env:
          WIZ_CLIENT_ID: ${{ secrets.WIZ_CLIENT_ID }}
          WIZ_CLIENT_SECRET: ${{ secrets.WIZ_CLIENT_SECRET }}

      - name: Run wiz-cli docker image scan
        run: ./wizcli docker scan --image ${{ needs.build.outputs.imageid }} --policy "$POLICY"

      - name: Fetch digest of Docker image for Graph enrichment
        run: |
          ./wizcli docker tag --image ${{ needs.build.outputs.digest }}